# Base URL for the (stub) API — used by the client
IBERITAX_API_URL=http://localhost:4000
# Optional API key header (not used by stubs but wired)
IBERITAX_API_KEY=dev-key

STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
GOOGLE_CLIENT_ID=
