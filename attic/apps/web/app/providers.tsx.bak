'use client'

import React from 'react'
import { AuthStateProvider } from '@/lib/client/authState'

export default function Providers({ children }: { children: React.ReactNode }) {
  return <AuthStateProvider>{children}</AuthStateProvider>
}
