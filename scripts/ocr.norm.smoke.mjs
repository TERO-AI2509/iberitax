import fs from "node:fs"; import { ocrAndNormalizePdf } from "../packages/ocr/dist/index.js"; const pdf=fs.readFileSync("tests/fixtures/ocr/scanned.pdf"); ocrAndNormalizePdf(pdf,{lang:process.env.OCR_LANG||"eng"}).then(t=>{ console.log(t.slice(0,200)); const ok = t.toLowerCase().includes("hola") && t.toLowerCase().includes("adios"); if(!ok) process.exitCode=1; }).catch(e=>{ console.error(e); process.exitCode=1; });
