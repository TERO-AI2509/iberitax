name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
name: SLA Check
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
on:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
  push:
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
  pull_request:
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
    branches: [ main ]
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
permissions:
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
  contents: read
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
jobs:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
  sla:
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
          node-version: '20'
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
      - name: Compute SLA JSON/CSV
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
        run: node scripts/modelo100.rules.meta.sla.mjs
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
      - name: Enforce SLA
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        id: enforce
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
        run: node scripts/modelo100.rules.meta.ci.sla.mjs
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Compute escalation (owners)
      - name: Compute escalation (owners)
        if: always()
        if: always()
        run: node scripts/modelo100.sla.escalation.mjs
        run: node scripts/modelo100.sla.escalation.mjs
      - name: Annotate escalation (owners)
      - name: Annotate escalation (owners)
      - name: Build owner routes (queue)
        if: always()
        run: node scripts/modelo100.routes.build.mjs
      - name: Notify owner routes (dry-run by default)
        if: always()
        env:
          NOTIFY_ENABLE: ${{ env.NOTIFY_ENABLE }}
        run: node scripts/modelo100.routes.notify.mjs || true
        if: always()
        if: always()
        env:
        env:
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
        run: node scripts/modelo100.escalation.annotate.mjs
        run: node scripts/modelo100.escalation.annotate.mjs
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Annotate SLA
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
      - name: Upload SLA artifacts
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        uses: actions/upload-artifact@v4
        with:
        with:
        with:
        with:
        with:
        with:
        with:
        with:
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          name: modelo100-sla-artifacts
          path: |
          path: |
          path: |
          path: |
          path: |
          path: |
          path: |
          path: |
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.csv
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.meta.sla.json
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.insights.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
            artifacts/modelo100/rules.dashboard.html
          retention-days: 7
          retention-days: 7
          retention-days: 7
          retention-days: 7
          retention-days: 7
          retention-days: 7
          retention-days: 7
          retention-days: 7
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        if: always()
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          GITHUB_ACTIONS: 'true'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
          SLA_TOP: '15'
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
        run: node scripts/modelo100.sla.annotate.mjs
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Job Summary
      - name: Job Summary
      - name: Job Summary
      - name: Job Summary
        if: always()
        if: always()
        if: always()
        if: always()
        run: |
        run: |
        run: |
        run: |
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          echo "## SLA Summary" >> $GITHUB_STEP_SUMMARY
          echo "## SLA Summary" >> $GITHUB_STEP_SUMMARY
          echo "## SLA Summary" >> $GITHUB_STEP_SUMMARY
          echo "## SLA Summary" >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          jq -r "\"- Total: \(.summary.total)\n- Breaches: \(.summary.breaches)\n- OK: \(.summary.ok)\n- Ignored: \(.summary.ignored)\n- Unknown: \(.summary.unknown)\"" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r "\"- Total: \(.summary.total)\n- Breaches: \(.summary.breaches)\n- OK: \(.summary.ok)\n- Ignored: \(.summary.ignored)\n- Unknown: \(.summary.unknown)\"" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r "\"- Total: \(.summary.total)\n- Breaches: \(.summary.breaches)\n- OK: \(.summary.ok)\n- Ignored: \(.summary.ignored)\n- Unknown: \(.summary.unknown)\"" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r "\"- Total: \(.summary.total)\n- Breaches: \(.summary.breaches)\n- OK: \(.summary.ok)\n- Ignored: \(.summary.ignored)\n- Unknown: \(.summary.unknown)\"" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY
          echo "### Top Breaches" >> $GITHUB_STEP_SUMMARY
          echo "### Top Breaches" >> $GITHUB_STEP_SUMMARY
          echo "### Top Breaches" >> $GITHUB_STEP_SUMMARY
          echo "### Top Breaches" >> $GITHUB_STEP_SUMMARY
          jq -r ".breaches[:10] | map(\"- \(.id) (sev=\(.severity//\"n/a\"), age=\(.age_days//\"?\")d, thr=\(.threshold_days//\"?\")d)\") | .[]" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r ".breaches[:10] | map(\"- \(.id) (sev=\(.severity//\"n/a\"), age=\(.age_days//\"?\")d, thr=\(.threshold_days//\"?\")d)\") | .[]" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r ".breaches[:10] | map(\"- \(.id) (sev=\(.severity//\"n/a\"), age=\(.age_days//\"?\")d, thr=\(.threshold_days//\"?\")d)\") | .[]" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
          jq -r ".breaches[:10] | map(\"- \(.id) (sev=\(.severity//\"n/a\"), age=\(.age_days//\"?\")d, thr=\(.threshold_days//\"?\")d)\") | .[]" /tmp/sla.json >> $GITHUB_STEP_SUMMARY
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
      - name: Optional Webhook notify
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        if: always() && env.SLA_WEBHOOK_URL != ''
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
        env:
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
          SLA_WEBHOOK_URL: ${{ secrets.SLA_WEBHOOK_URL }}
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
        run: |
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          node scripts/modelo100.sla.summary.json.mjs > /tmp/sla.json
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
          curl -sS -X POST -H 'Content-Type: application/json' --data @/tmp/sla.json "$SLA_WEBHOOK_URL" || true
